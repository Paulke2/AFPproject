const Calendar = () =>{
return(<>here i plan to make a calendar to schedule ppl to projects. option to complete week which totals ppls hours at each
job for the week. output cvs/exel file for each month if user presses a button. </>);
}
export default Calendar;